import CloverDotInpageProvider from "./dot-inpage-provider";
import CloverInpageProvider from "./inpage-provider";
import { CloverParams } from "./interfaces";
import ExtendedObjectMultiplex from "./ObjectMultiplex";
import CloverSolInpageProvider from "./sol-inpage-provider";
declare class CloverWebInjected {
    cloverIframe: HTMLIFrameElement;
    zIndex: number;
    cloverWebUrl: string;
    devEnv: boolean;
    initialized: boolean;
    requestedVerifier: string;
    currentVerifier: string;
    communicationMux: ExtendedObjectMultiplex;
    isLoginCallback: () => void;
    isLoggedIn: boolean;
    ethereum: CloverInpageProvider;
    clover_solana: CloverSolInpageProvider;
    clover_polkadot: CloverDotInpageProvider;
    provider: CloverInpageProvider;
    dappStorageKey: string;
    constructor({ zIndex, devEnv }?: {
        zIndex?: number;
        devEnv?: boolean;
    });
    init({ network, enableLogging, }?: CloverParams): Promise<void>;
    private displayIframe;
    private setupWeb3;
    solLogin({ verifier }?: {
        verifier?: string;
    }): Promise<string[]>;
    polkadotLogin({ verifier }?: {
        verifier?: string;
    }): void;
    login({ verifier }?: {
        verifier?: string;
    }): Promise<string[]>;
    logout(): Promise<void>;
    cleanUp(): Promise<void>;
    clearInit(): void;
    /** @ignore */
    _setProvider({ chainId }?: {
        chainId?: string;
    }): Promise<void>;
    /** @ignore */
    _showLoginPopup(calledFromEmbed: boolean, resolve: (a: string[]) => void, reject: (err: Error) => void, chainName?: string, polkadotObj?: any): void;
    /** @ignore */
    _handleWindow(preopenInstanceId: string, { url, target, features }?: {
        url?: string;
        target?: string;
        features?: string;
    }): void;
}
export default CloverWebInjected;
